{
  "name": "ai-status",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Statut opérationnel des fournisseurs de modèles IA — site statique GitHub Pages",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "collect": "node collect.mjs",
    "test": "node test/test.mjs && node test/frontend.mjs",
    "serve": "python3 -m http.server 8080 -d public"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
